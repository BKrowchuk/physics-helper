import classes from "./PhysicsList.module.css";
import Card from "../UI/Card";
const PhysicsList = () => {
  return (
    <section className={classes.section}>
      <Card>
        <p>this is a list of physics tools</p>
      </Card>
    </section>
  );
};

export default PhysicsList;
